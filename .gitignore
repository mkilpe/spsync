# build trees (out-of-source only; see CMakePresets.json and build-android.sh)
build/
build-*/
build_*/
compile_commands.json

# stray in-source cmake leftovers (an old in-source build left Makefiles in the source
# directories; none is tracked)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Testing/
*_autogen/
Makefile

# leftovers of the old in-source build system (bin/, lib/ and the local cmake.config)
/bin/
/lib/
cmake.config

# editors and scratch
*.swp
*.log
*.sublime-project
*.sublime-workspace
.cache/

# test binaries run from the repo root leave these behind (all regenerated)
/*.db
:memory
/record-storages/
/test-storage-root*/
/test-s2s-*/
/client_*/
/gc_test_c*/
/gc_test_server/
/json_test_server/

# asciidoctor-diagram intermediate image cache
doc/diagrams/diag-*.png
doc/diagrams/diag-*.svg
.asciidoctor/
