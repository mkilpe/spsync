
* try to test everything possible with reasonable effort
* design all components to be easily testable in as much isolation as possible

* four kind of tests:
	- normal unit/system tests (divide by unit and system tests by tag? tags [unit] [system])
	- security related tests (flip a bit in crypto etc) tag: [security]
	- stress tests (randomised timing/input/output, might run indefinitely) tag: [stress]
	- performance tests (decide/record baseline and make test of the most important things based on that) tag: [performance]