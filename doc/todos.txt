
= client_sync
* state when creating channel, when it was successful, failed, pending
* someway to store and check the status / retry if failed
* handle joinin failed/creating failed callback / info for user

= records
* get history for higher levels
+ allow to give tag of the first record (or some other record?) to engine when joining a storage to check the authenticity
  (done 2026-09-25, distributed plan 5.5: the invitation's first block is the engine's trusted anchor)
* allow to start local chain from given record, so that when someone joins, he needs to only anchor the user change that contains the key for him and everything after that
* support signing records

= client
* groupchat::join_chat loads the chat connection but does not connect it; every caller
  (json_manager, tests) has to connect it first - join_chat should do it (found 2026-08-27)
* better interface to handle pending/in sync and error cases on client level
* limit amount of chats (created+joined=limit)
* remove chat

= general
* provide server support to send message to a person (potentially on different server)
* provide side channel for storages groups to send non-stored packets to each others (eg. "writing a message" in gc)

= tests
+ every fixed test port (42750-42830) lay inside Linux's ephemeral range (32768-60999): an
  outgoing connection of ANY process can sit on one and the test that binds it fails with
  "bind: Address already in use" (seen 2026-09-21: another process held 42780 for many
  minutes, "s2s replica creates the storage on first contact" failed every run and took
  "s2s bootstrap a new replica" with it). Fixed 2026-09-25: the test servers take slots of
  spsync/test/test_ports.hpp (25700 and up, below the range), a lone data role binds port 0;
  the product defaults (18188-18203) were below the range all along; a held test port now
  fails that one test only (tried 2026-09-25 with slot 8's client port held: 9 of 10 pass)
* test_server (spsync/test/test_server_runner.hpp) listens when run() returns since
  2026-09-25 (spsync_server::run starts the roles synchronously): no sleep after it

= server
* commit throughput is sqlite-fsync bound (~85 signed commits/s per storage, measured
  2026-08-29 on 9950X); WAL + synchronous=NORMAL or commit batching moves it towards the
  crypto ceiling (~2850/s single-threaded). ML-DSA-65 verify itself is 114us.
  Benchmark: test_spsync_server_lib "[.performance]" (hidden tag, not run by ctest)
* update and check user access rights for storage when handling records
* improve event handling (i.e. try to remove blocking mutexes with strands etc)
* gather statistics
* enforce max sizes for records and a like
* check signature/sender
* separate public key storage for "own" and "foreign" keys
  * allow only own keys to connect to packet server
* allow to throttle connection speeds

---------------------

* correct member change notifications
* change members for existing storage
  * how to get the encryption key for the new member
    * use msg server (below)
    * try to find the key from the member change before processing all the records

* signing [1d]
  + allow to sign
  + check on receive if we have key
  * client interface to check if record was signed and authentic (if we don't have the key, it is not checked)
  * optional server side check of the signature?
* msg server [5d]
  + separate server to send messages to other users
  + stored in database and acked when received
  * server connects to the server of receiver to transport the message
* push notification [2d]
  * server side impl
  * required client/protocol support
* side channel [2d]
  * use storage connection to send broadcast message to all online members (msg not saved on server side)
  * client side interface to send and receive such messages
  * message encrypted using storage encryption key?
* review of the record data work (doc/review_2026-09-21.txt): F1-F14 fixed, duplication
  D1-D7 and test helpers T1 fixed 2026-09-22, robustness O1-O9 fixed 2026-09-23 (liveness
  on the data links and ticket requests, retry vs reset, detach, staged pieces and sessions
  capped, one digest per chunk, idle transfers trimmed, rounds on the io context), dead code
  C1/C2/C4/C5 fixed 2026-09-24, tests T2-T4 (long test cases, fixed sleeps, ports)
  2026-09-25; nothing open
* architecture review 2026-09-24 (doc/review_2026-09-24_architecture.txt): R1 done (the
  transfer machinery is spsync/transfer, server_lib no longer links comm, comm no longer
  links transfer: they meet in core/data/data_transfers.hpp, the client wires
  net_data_transfers in when it has a data store), R2 done (wire_modes is
  protocol/wire_modes.hpp), R4 done (server_lib/data_coordinator behind storage_data_context,
  storage_server::impl is the record role again), R5 done (engine/data_sync,
  core/data/data_index), R6 done (encrypted_connection::close_later in securepath, the
  three post_on_strand shutdowns use it; post_on_strand kept); nothing open
+ securepath: encrypted_connection::close_later (added 2026-09-24, R6) committed in the
  submodule 2026-09-25 (105b462) and used by spsync (d440637); post_on_strand (added
  2026-09-23 for the timer driven closes, unused by spsync since R6) stays, like the
  packet_deserialiser ctor forwarding before it
* review backlog (see doc/review_2026-09-08.txt, "Backlog as of 2026-09-09")
  - R1 groupchat blocking create_account/register_my_key, swallowed on_create/connect errors
  - R2 strict mode on a different replica (phase 6)
  - D1-D9 duplication and dead code, D1 candidate-key helper first

* bigger groups: a user change that gives the key to every member (key rotation after a
  removal) costs about 1.3 KB per member and a record is at most 2 MiB (about 1600 members,
  800 with the default 1 MiB); the user change record needs a way to change keys without
  an envelope for each member, or another mechanism - options in doc/user_changes.txt
  "Scaling: members per user change"; segment records have the same ceiling (tag list)
* record data: a release_data a data server missed is made up for when it announces the data
  again (RDS 10) - except for data a REPLICATED storage does not know (rollback, cut away
  records): that could be a record that has not arrived yet, so nothing is released; needs a
  statement all record servers of the storage agree on (record_data.txt RDS 10)
* record data: parallel ranges of one data from several holders in the client's downloader;
  a full data server stepping off the placement ring for new copies; "held by k replicas" as
  a client visible data state (record_data.txt RDS 10 "Not done")
+ packet size caps on the record and s2s connections (found 2026-09-19 with record_data.txt
  RDS 5, fixed 2026-09-21): the deserialisers of the client's record connection, the server's
  client connection and the peer connection take messages up to the transport frame; the
  batch budget is 1 MiB with at least two records per batch (a fetch continues FROM the
  record held, a batch of one never got past it - the actual wedge); max_record_size default
  1 MiB, maximum 2 MiB (what the codec carries for one record); the engine does not repeat
  a fetch that made no progress
