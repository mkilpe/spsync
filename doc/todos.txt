
= client_sync
* state when creating channel, when it was successful, failed, pending
* someway to store and check the status / retry if failed
* handle joinin failed/creating failed callback / info for user

= records
* get history for higher levels
* allow to give tag of the first record (or some other record?) to engine when joining a storage to check the authenticity
* allow to start local chain from given record, so that when someone joins, he needs to only anchor the user change that contains the key for him and everything after that
* support signing records

= client
* better interface to handle pending/in sync and error cases on client level

= general
* provide server support to send message to a person (potentially on different server)
* provide side channel for storages groups to send non-stored packets to each others (eg. "writing a message" in gc)

= server
* update and check user access rights for storage when handling records
* improve event handling (i.e. try to remove blocking mutexes with strands etc)

out of sync:

  1. bring us up-to-date with server state

  2. see if there are conflicts and notify higher level if there are
  3. recreate the records with correct previous tag/last seen seq for non-conflicting records
  4. try to commit again

---------------------
* get_messages chunking [??]
  * mapping from record(s) to a message
  * need way to order committed and pending records
  * chunking based on index
  * notice that record order can change due to records from server

* correct member change notifications
* change members for existing storage
  * how to get the encryption key for the new member
    * use msg server (below)
    * try to find the key from the member change before processing all the records

* signing [1d]
  * allow to sign
  * check on receive if we have key
  * client interface to check if record was signed and authentic (if we don't have the key, it is not checked)
  * optional server side check of the signature?
* msg server [5d]
  * separate server to send messages to other users
  * stored in database and acked when received
  * server connects to the server of receiver to transport the message
* push notification [2d]
  * server side impl
  * required client/protocol support
* side channel [2d]
  * use storage connection to send broadcast message to all online members (msg not saved on server side)
  * client side interface to send and receive such messages
  * message encrypted using storage encryption key?

