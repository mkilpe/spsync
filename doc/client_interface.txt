

* how to handle connect/disconnect from network?
* how to handle conflicts?
	* notify the higher layer but what we do with the conflicted local record?
* what is the threading/call strategy for the engine?
	* eg. do we allow to higher level to call engine from the callbacks that are called from the engine?
	* should the comm layer never call directly back?
