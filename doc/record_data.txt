SPSync record data support plan
===============================

Markers as in dev-phases.txt:  + done   * in progress   - todo
Written 2026-08-30, decisions RD11-RD13 and the resolved questions added 2026-09-07
(after distributed phase 4 / M3). Cross-references: use_cases.txt (1: file-like
sync on mobile), progress_indication.txt, segments.txt (S6 retention),
distributed_sync.txt (D1 content-blind servers, phase 4 s2s), data_changes.txt,
dev-phases.txt phase 2.


Purpose
-------

Records today carry only metadata; the actual payload ("record data") of an object
change - a file, an attachment, any biggish blob - has an interface
(core/record_data.hpp), a state machine, protocol stubs (request_data/response_data,
server replies not_implemented) and a data_change_info field, but no implementation
anywhere. This plan makes record data real, sized for file sync / file transfer:
multi-gigabyte objects, resumable transfers, lazy per-object fetching on mobile
(use case 1), verifiable end to end while the server stays content-blind (D1).

Data is deliberately NOT part of the record chain: the record authenticates a
descriptor of the data, the bytes travel and are stored out of band. Committing a
record never waits for its data upload; readers see the record immediately and the
data state separately (record_data_state::remote_not_complete exists for this).


Current state (2026-08-30)
--------------------------

Exists:
  - core/record_data.hpp: record_data interface (offset read/write, remove_data)
    and record_data_state {deferred, remote_not_complete, upload_pending,
    download_pending, in_sync, removed, invalid} - the states already match the
    flow below.
  - data_change_header: optional data_change_info{size, iv, gcm_tag} inside the
    ENCRYPTED header - designed for one GCM pass over the whole blob (64 GiB cap,
    no random access, server cannot verify anything). Superseded by RD2/RD3.
  - protocol: request_data{record seq, start, end} / empty response_data; server
    connection replies not_implemented; comm::handle(response_data) stub.
  - record_objects.data_ref column in the client record storage, never set.
  - crypto: auth_stream_cipher (streaming AES-GCM with AAD), sha3, PQ signing.
  - engine: sync_object_change(oid, metadata, record_data_handle = {}) already
    accepts a data handle and drops it.

Missing: everything behind those seams - no data store on either side, no chunking,
no upload/download, no verification, no lifecycle.


Design decisions
----------------

RD1 Separation of record and data. The record chain stays small and fast to sync.
    A single_change may reference one data blob through a descriptor that is part
    of the authenticated record; the blob itself is chunked, stored and moved
    independently. Record commit never blocks on data (mobile, resume, "message
    before attachment"); consumers must tolerate remote_not_complete.

RD2 Descriptor, two views (replaces data_change_info):
      plain part of single_change (server-visible, authenticated):
        enc_size          total encrypted size (quota checks, progress)
        chunk_size        chunking used for this data
        manifest_digest   sha3-512 over the ordered list of chunk digests
      encrypted header (members only):
        plain_size        plaintext size
        content_digest    sha3-512 of the plaintext (end-to-end integrity, later
                          dedup basis)
    The server-visible half leaks only sizes (which traffic reveals anyway) and
    lets the server fully verify stored bytes without decrypting: the client sends
    the chunk digest list (manifest) first, the server checks it against
    manifest_digest, then checks every chunk against the manifest. Junk uploads
    and bit rot die at the server's edge; s2s replication gets the same check for
    free. data_id := manifest_digest (stable across rebases, see RD7).

RD3 Chunked encryption. Chunks of chunk_size (default 1 MiB, per-storage config)
    encrypted independently:
      data_key   derived per data from the group encryption key and a random
                 per-data nonce (HKDF over sha3); key separation means GCM nonce
                 management is local to one data
      nonce      chunk counter
      AAD        data nonce + chunk number (no reorder/transplant)
      per chunk  AES-256-GCM tag stored with the chunk
    The per-data nonce and encryption key sequence live in the encrypted header.
    Chunking removes the old 64 GiB single-GCM cap and gives random access:
    reading offset X decrypts only the chunks covering X; a failed chunk marks the
    data invalid, not the record.

RD4 Transfer protocol (flesh out the c2s stubs; all inside the encrypted
    transport as today):
      upload_data_manifest {tag, change idx, chunk digests}   opens an upload,
          server verifies against the committed record's manifest_digest;
          response says which chunks it already has (resume)
      upload_data_chunk {data_id, chunk no, bytes}            server verifies the
          digest, stores, acks with a window to keep pipelining
      request_data {tag, change idx, chunk range}             replaces the seq
          based stub; response_data streams the chunks it has and states
          completeness
      notify_data {sid, tag, change idx, complete}            to storage listeners
          when a data becomes complete, so waiting clients fetch without polling
    Uploads happen after the record is acked (the tag exists server side);
    interrupted transfers resume by manifest. Fetch is pull-based and lazy by
    default (use case 1: fetch data for individual files only).
    Wire pieces (2026-09-19): the chunk is the unit at rest - one file, one
    AES-GCM pass, one manifest entry, the resume granularity - and must not be
    bound by what a packet may carry (the DER codec takes one octet string of
    2 MiB at most, and whole chunks in packets cost their size in memory per
    packet in flight). A chunk therefore travels in pieces: {chunk no, offset,
    bytes}, 128 KiB by default, at most 1 MiB, in order from offset 0. The
    sender reads pieces from the chunk file, the receiver appends them to a
    staged file and hashes as they come; the piece that completes the chunk
    gets the whole checked against the manifest and only then it becomes a
    chunk. A gap, an overrun or wrong bytes lose the chunk (it starts over at
    offset 0), a lost connection loses the chunks that were half way. So
    max_chunk_size stays 8 MiB and the memory a transfer takes is the window
    of pieces, whatever the chunk size.

RD5 Server data store. Chunks as files under the storage directory
    (record-storages/<sid>/data/<data_id>/...), sqlite is for the bookkeeping
    only: data table {data_id, record tag, change idx, enc_size, chunk_size,
    manifest, have-bitmap, complete}. The server never decrypts (D1). Limits and
    quota per RD10; incomplete uploads expire. Chunks of records that are pruned
    at a segment cut are deleted unless a retained record still references the
    same data_id (segments.txt S6).

RD6 Client data store. One record_data implementation over the same chunk-file
    layout plus local state (record_objects.data_ref finally points at a client
    data table row: {data_id, state, have-bitmap}). Policy per storage: lazy
    (deferred until asked - mobile default) or auto-fetch up to a size threshold.
    read() decrypts chunk-wise and verifies on the fly; locally created data is
    written through the store's data_writer before upload (RDS 2: the data_id
    exists only after the last chunk, so a stored record_data is immutable);
    remove_data() evicts local chunks (state removed, refetchable) without
    touching the record.

RD7 Engine flow. sync_object_change with a data handle: stream the source once -
    encrypt chunks into the local store, build the manifest, put the descriptor
    into the record - then commit as usual; comm uploads pending data in the
    background (few in flight, resumable) and flips upload_pending -> in_sync.
    Incoming records with a descriptor surface as deferred/remote_not_complete;
    explicit fetch (or auto policy) drives download_pending -> in_sync. Rebase
    reuses the data untouched: the descriptor is inside the re-signed record and
    data_id/chunks do not depend on the record tag. New engine events:
    on_data_state_changed, on_data_progress (progress_indication.txt wants per
    entity upload/download progress).

RD8 Replication (details belong to distributed_sync.txt): data is not in the
    chain_log and not in the Raft log. Only servers with the data role (RD12) hold
    chunks; they announce availability per data_id (RD13) and replicate chunks
    among themselves by data_id manifest - same verification path as clients -
    according to the copy count of RD13. Record servers never carry bulk bytes.
    Weak and strict mode identical here.

RD9 Lifecycle. A newer change to the same oid supersedes the old data (old one
    becomes prunable at the next segment cut unless retained); remove-type changes
    (the open question in single_change) make the object's data dead. Local
    eviction (RD6) is independent of server retention. Data of records dropped by
    truncate_from (rollback) is dropped with them once no record references the
    data_id.

RD10 Quotas and limits - data AND the record chain. Today the only bound is the
    transport frame cap (16 MiB payload per encrypted frame, framing.hpp): no
    validation cap, record metadata is arbitrary octets, so the chain can be
    bloated as easily as data. Worse, the frame cap plus batching is a wedge
    hazard: response_records packs up to max_returned_records (30) blocks into
    ONE frame, so records above ~max_frame_size/30 (~0.5 MiB) can make a range
    unsendable - each committed fine alone, the batch can never be fetched.
    max_record_size must stay well below max_frame_size/max_returned_records and
    the record batching should become size-aware. Two kinds of limit with
    different rules:

    Validity limits (deterministic, replicated): anything that makes a record
    invalid must be judged identically by every replica, or weak-mode replicas
    diverge and a Raft leader/follower disagree on apply. So max_record_size (and
    the existing max data descriptor bounds) are per-storage creation parameters
    next to storage_modes: wire-encoded in create_storage, persisted in
    storage_config, immutable, checked in chain_sync validation ->
    protocol::errc::record_too_big. Server operator sets the defaults for new
    storages (spsync_server.cfg).

    Resource quota (local, per server): total bytes of stored data, max single
    data size, per-member and total chain growth budgets. These do NOT affect
    record validity - a server may refuse to *accept an upload* or *serve as
    origin* (quota_exceeded, data_too_big) without disagreeing about what a valid
    record is; data completeness already differs per server (RD8), so a peer
    hitting its disk limit degrades availability, not consistency. Enforced at
    the protocol edge: upload_data_manifest/chunk against data quota, commit
    against a chain budget. Accounting is incremental per storage (chain bytes,
    data bytes) in the storage db, recomputed at segment cuts.

    Transfer quota (local, per server): traffic budgets on top of the resource
    quota - bytes uploaded/downloaded per storage (and per member once 2.3 signer
    attribution is wired in) over a time window. Storage quota bounds what is
    kept, transfer quota bounds what is moved: repeated re-downloads of the same
    data cost no storage but real bandwidth, so data serving (request_data) is
    the main consumer. Same nature as the resource quota: server-local,
    accounted incrementally per window in the storage db, refusal
    (quota_exceeded, retry-after) degrades availability only - a client resumes
    in the next window, replication peers (RD8) fall behind but never diverge.
    Chain sync (records) should stay outside the transfer budget or get a
    generous separate one: starving record sync breaks liveness for kilobytes.

    The chain's long-term size control is retention, not quota: history is cut at
    segments (segments.txt S6) and dead data pruned with it; the chain budget
    only guards the growth rate between cuts. Clients learn the limits in the
    storage info exchange (response_sequence_number carries the modes already;
    add the limits) so the engine can refuse oversized changes before committing.
    Per-connection instantaneous rate limiting (bytes/s) is operational
    hardening, phase 7 territory; the transfer quota above is the cumulative
    counterpart.

RD11 Size hiding (2026-09-07). The server must know the ciphertext size and the
    chunk count (it stores, verifies, serves ranges and counts quota) and traffic
    volume reveals the same to anyone on the path (the securepath threat model
    keeps traffic analysis out of scope), so the achievable goal is: the server
    learns a size class, never the plaintext length.
      - Data is padded before chunking, inside the ciphertext, with a size-class
        rule (Padme style: the padded length keeps only a few significant bits;
        overhead capped at ~12% and shrinking with size, floor 4 KiB). Padding
        to chunk_size would turn a 3 KB file into 1 MiB. plain_size in the
        encrypted header is the true length, so readers strip the padding and
        progress stays exact; enc_size is the padded ciphertext size and quota
        counts that.
      - The encrypted record header gets a trailing padding field inside the
        AEAD with the same rule: message texts and inline payloads leak only a
        size class. Cheap, independent of data support, needs the record
        structure version bump.
      - Accepted leaks: chunk count (coarse once padded), the presence of a
        descriptor, access patterns. No dummy data or cover traffic at this
        layer.
    The rest of RD3 stands; the manifest digest in the authenticated record
    already stops a server from truncating or reordering chunks.

RD12 Server roles, data endpoints, tickets (2026-09-07). Bulk bytes never pass
    through a server that will not keep them (servers are traffic bound, traffic
    costs money): no pull-through proxying.
      roles       every server has the record role (chain, commits, s2s heads)
                  and optionally the data role (chunk store). Same binary, same
                  protocol; the data role is its own listener and port so it can
                  sit on another box or link. Deployments: record servers A, B
                  plus data server D; a data server set D1..Dn; or A, B with both
                  roles (the all-in-one replicas of distributed phase 4).
      endpoints   the storage info exchange (the sequence response carries the
                  modes already; add the RD10 limits and the data endpoints)
                  lists the data-role servers of the storage: host, port, key id,
                  optional region label. Cluster configuration on the record
                  servers next to the peer list; the client persists it per
                  storage like the cursor owner.
      direct      clients upload to and download from data servers directly; in
                  the all-in-one setup a client on B wanting data only A holds is
                  told that A has it (redirect, not proxy). Proxying is an
                  explicit fallback for a client that cannot reach any holder,
                  off by default.
      tickets     a data server has no chain, so it can check neither membership
                  nor a manifest against a committed record. The record server
                  issues a signed data_ticket {sid, data_id, manifest_digest,
                  enc_size, chunk_size, member key id, right (upload/download),
                  expiry}; the client presents it, the data server verifies the
                  record server signature (it trusts record server keys the way
                  peers trust each other on hello) and then verifies manifest and
                  chunks exactly as RD4. Quota accounting happens where the ticket
                  is issued. The data server stays blind to content and history;
                  an all-in-one server issues tickets to itself.
      client auth the data listener requires certified client keys (as the
                  packet server does); no shared-secret variant.

RD13 Availability, holder lists, load balancing, copy count (2026-09-07). Two
    pieces of "where is the data", in two places:
      endpoint list   static, RD12 (which servers may hold data of the storage).
      holder list     dynamic: data servers announce availability per data_id
                      (have-bitmap, complete flag, plus load signals: free space,
                      bytes in flight) to the record servers they are configured
                      with, when chunks complete and again in anti-entropy;
                      record servers keep a per-storage availability table and
                      relay it to each other with the heads. The ticket response
                      carries the ordered holder list of the data; the ticket is
                      valid at any holder (it names the data, not the server).
    Holder lists are transient, a fresh ticket always gives the current view; the
    client persists only its own data state and have-bitmap.
    Load balancing is the ordering of that list, decided by the record server
    which issues every ticket anyway:
      downloads   holders ordered by completeness, load, locality; a client may
                  fetch chunk ranges of one data from several holders in parallel
                  (every chunk verifies on its own, the manifest fixes the set).
      uploads     the upload ticket names the preferred data servers in order:
                  round robin / least loaded for ingress, a consistent hash of the
                  data_id for even storage across a set.
      failover    a holder that is down is the next entry; none reachable ->
                  remote_not_complete and a retry with the next ticket.
    Copy count k per storage class instead of eager/on-demand modes: the primary
    holders of a data are the first k servers on the data_id hash ring, data
    servers replicate among themselves until k copies exist, clients download
    from any holder. k = number of data servers is full replication, k = 1 a
    single copy (durability = that disk, no cross-server traffic), k = 2 the
    usual choice. k is server-side policy (operators know the disks), not a
    storage mode; a data server at its quota behaves as if not on the ring for
    new data - availability degrades, nothing diverges. The granularity of the
    balancing is per data and per ticket; finer is the client's chunk-range
    choice. A deployment with one data server degenerates to a list of one.
    Later: surface "held by k replicas" as a data state, the way records
    distinguish acked from in_sync (D8).


Implementation steps
--------------------

+ RDS 1  Descriptor and chunk crypto -- done 2026-09-10                        [1d]
      Descriptor v2 in single_change + encrypted header (RD2), per-data key
      derivation, chunker with manifest build/verify (RD3), size-class padding of
      the data and the trailing padding field of the encrypted record header
      (RD11, structure version bump). Unit tests: round trip, tamper per chunk,
      reorder, wrong manifest, padding classes and stripping, header padding
      invisible to readers. data_change_info retired.
      + core/data/: size_padding (Padme, constexpr; data floor 4 KiB, header
        floor 256 B - "the same rule" of RD11 with a smaller floor so a chat
        message costs ~250 B of chain, not 4 KiB), data_descriptor {enc_size,
        chunk_size, manifest_digest} in plain_single_change_data and data_header
        {plain_size, content_digest, nonce, key_seq, flags} in
        data_change_header, chunk_crypto (HKDF-SHA3-512 data key from group key
        + 16 B nonce, iv = chunk counter, AAD = nonce + counter, tag appended),
        data_manifest (digest, matches(descriptor), verify_chunk),
        data_encryptor (streaming: write() any pieces, chunks to a sink as they
        complete, finish() pads inside the ciphertext and returns descriptor +
        header + manifest) and data_decryptor (any chunk alone, padding
        stripped by plain_size). chunk_count derives from enc_size and
        chunk_size (each chunk carries a tag), nothing extra on the wire.
      + encrypted record headers (all three record kinds) are padded_record_header
        {header, padding} inside the AEAD; record_base::current_structure_version
        = 2, verifiers report supported() and never read another version
        (is_authentic false, headers empty) - old records are not migrated.
      + securepath crypto::hash_stream (incremental sha3) for content and manifest
        digests over streams.
      + tests: core test_record_data_crypto (padding classes, chunk binding,
        round trip incl. 0 bytes and chunk boundaries, tamper/reorder/transplant/
        foreign manifest/wrong key, random access, descriptor serialisation,
        header padding classes), engine creator/verifier (descriptor halves and
        their authentication, padding invisible + size classes, version gate).

+ RDS 2  Client data store -- done 2026-09-18                               [1.5d]
      record_data implementation over chunk files + state table, data_ref wiring
      in record storage, eviction, data_id reference counting. Tests: offset
      read/write across chunk boundaries, remove/refetch state, big-file
      streaming without loading whole blobs.
      + core/data/, four pieces a data server can reuse (RDS 4): have_bitmap
        (one bit per chunk, the persisted/wire octets normalised), chunk_files
        (<root>/<hex data_id>/<chunk no, 8 hex digits>, temp + rename so a chunk
        file is complete or absent; a staging area under <root>/.staging for a
        data whose id is not known before its last chunk, renamed on commit,
        cleared when the store opens), data_state_table (sqlite table
        record_data {key, data_id unique, enc_size, chunk_size, state, have,
        manifest}) and record_data_store on top of them.
      + creating: store.create(group key, chunk size) gives a data_writer -
        write() plaintext in any pieces, chunks are encrypted into the staging
        area as they complete, finish() registers the data as upload_pending
        with everything held and the manifest kept for the upload. The row is
        written before the rename, so a crash leaves an unreferenced row for
        the sweep and never chunk files nothing knows of. This replaces the
        "write() for locally created data" of RD6: a stored data is what its
        descriptor says, record_data::write on it throws.
      + reading: store.open(key candidates, descriptor, header) gives the
        record_data handle; read() at any offset decrypts only the covering
        chunks (the last one is cached, small sequential reads decrypt each
        chunk once), stops at a chunk that is not held, size() is plain_size,
        available_size() the data octets in held chunks (not necessarily
        contiguous). The candidates are the group keys of the header's key_seq
        (D9 collisions); the one that authenticates is found on the first
        chunk. A descriptor contradicting the row of its data_id opens nothing.
      + failed chunk (RD3), two cases told apart by the manifest: a held chunk
        that is no longer the one the manifest names (bit rot, lost file) is
        dropped and the data goes download_pending - refetchable; a chunk the
        manifest names but no key authenticates makes the data invalid. While
        upload_pending (the only copy) both are invalid.
      + transfer side, ciphertext only: set_manifest (must match the
        descriptor), store_chunk (verified against the manifest, so a download
        fetches the manifest first - detail for RDS 6; the last missing chunk
        flips the data to in_sync), read_chunk + manifest for the upload.
      + eviction: evict()/remove_data() drops the chunk files, state removed,
        row and manifest stay so the refetch verifies at once; refused while
        upload_pending.
      + references: record_storage stores the row of the data_id in
        record_objects.data_ref for every change with a descriptor (row created
        as deferred when the data is new - an incoming record - and left alone
        when known); data_ref is no longer unique, old tables are rebuilt on
        open. data_reference_count(local id) counts object rows of records in
        any state, so a rebase (set_record) and truncate_from/truncate_prefix
        move the count with the records; store.remove_unreferenced(predicate)
        sweeps rows + chunks nobody references (RD9) - the engine calls it
        after truncation/rebase, never between a writer's finish() and the
        creation of its record. Servers get the rows as well: the index of the
        data their chain names, which RDS 5 needs for tickets.
      + tests: core test_record_data_store (bitmap, chunk files + staging,
        table persistence, offset reads and pieced writes across chunk
        boundaries, empty data, evict/refetch with tampered and misplaced
        chunks, download into a second store in any order with a gap, key
        candidates, rot vs invalid, writer cleanup, sweep, 24 MiB streamed in
        64 KiB pieces and verified against content_digest) and
        test_record_storage (shared rows, rebase keeps the reference,
        truncation drops it, upgrade of the unique data_ref table).
      Not here: where the client keeps its data directory and the wiring into
      client_sync/engine (RDS 3), a content_digest index for the download skip
      (RDS 6), fsync of chunk files (decide with the server store, RDS 4).

+ RDS 3  Engine create path and upload -- done 2026-09-18                   [1.5d]
      sync_object_change streams source -> local chunks + descriptor; upload
      queue in comm (ticket, manifest, chunks, resume; two datas in flight, a
      window of 4-8 chunks, commit order), states and progress events. Tests:
      commit-then-upload ordering, interrupted upload resume.
      Scope: the data servers (RDS 4) and the tickets (RDS 5) do not exist yet,
      so the upload logic is complete and tested against an interface, the wire
      packets and the real channel come with the servers that answer them. Until
      then a real client's own data stays upload_pending (readable locally) and
      the upload attempt reports not_supported.
      + sources: core/data/source_record_data - memory_record_data and
        file_record_data (read-only), what an application hands to
        sync_object_change. The handle is a source only, streamed once in 256 KiB
        pieces by copy_record_data into a data_writer; it never becomes the
        stored data.
      + engine create path: the source is streamed and encrypted BEFORE the
        engine mutex is taken (a multi-gigabyte source must not stall the
        engine; the call itself takes that long, engine_input documents it),
        with the current group key and the storage's chunk size (limits learned
        on attach, 1 MiB default until then). Under the mutex: writer.finish(),
        descriptor halves into the change, record created and committed - the
        sweep runs under the same mutex, so it never sees a finished data
        without its record. A change that cannot become a record (too big for
        max_record_size, no chain yet) sweeps its data away again; a source
        shorter than its size() is refused before anything is registered.
        Rebase keeps both descriptor halves untouched (update_record copies
        header and plain data), the reference moves with set_record.
      + upload trigger: request_uploads() asks the record storage for
        confirmed_data_in_state(upload_pending) - the upload_pending data that a
        server confirmed record (in_sync, acked) names, ordered by the sequence
        of its first record = commit order - and calls comm.upload_data(data_id)
        for what is not asked yet. One query covers every way a record gets
        confirmed (commit answer, the server's push beating it, a fetch) and the
        resume: it runs after a commit answer, a received record, a record
        fetch and the sequence answer of a (re)connect. on_disconnected forgets
        the requests; a failed upload is remembered for the connection and
        tried again after the next connect (no retry storm on a quota refusal).
      + answers: on_data_uploaded without error flips upload_pending -> in_sync
        in the store and emits engine_output::on_data_state_changed(data_id,
        state); with an error on_data_transfer_failed(data_id, error), state
        unchanged. Data is named by data_id in events (several records may share
        it). client_sync relays both and gets sync_engine_config::data_root: the
        chunk directory (record-storages/<sid>/data next to the database),
        empty = the storage keeps no record data and refuses changes with data.
      + reading: engine_input::object_data(record, change index) /
        client_sync::object_data - decrypts the change's header, takes the group
        keys of the data header's key_seq (D9 candidates; after a rebase the
        record key may be newer than the data key) and opens the store handle.
        Works for own data (upload_pending) and for data of others (deferred,
        size known, nothing held).
      + comm: comm_input::upload_data(data_id) -> request handle, answered by
        on_data_uploaded; comm_input::data() gives the engine the store (null =
        no record data). comm owns a data_uploader when it has a store and a
        data_channel (both optional parameters of create_storage_connection).
      + data_channel (comm/data_channel.hpp): open_upload(descriptor, manifest)
        -> the holder's have-bitmap, send_chunk(data_id, no, bytes) -> ack, both
        answered once through a callback from any thread. What is behind it -
        ticket from the record server, choice of the holder, data connection -
        is the implementation's business (RDS 4/5).
      + data_uploader (comm/data_uploader): queue in call order, max_datas = 2 on
        their way, per data manifest first, then only the chunks the holder
        lacks with window = 6 unanswered; resume is the same flow. A failure
        stops sending for that data and reports once the outstanding answers are
        in; the next data is not held up. reset() on disconnect forgets
        everything (generation counter: late answers are ignored, no callback),
        the engine queues again after the reconnect. The channel is never called
        with the lock held and may answer inside the call: rounds are flagged,
        never nested. It moves ciphertext only and does not touch data states.
      + progress: progress_events::on_data_progress(data_id, transferred, total,
        upload) on the storage's progress handler - encrypted octets known to
        be at the holder of enc_size, a resumed upload starts from what the
        holder had.
      + tests: comm test_data_uploader (fake holder answering at once, held, or
        from another thread: order and two at a time, window, resume from a
        partial bitmap, interrupted + re-queued sends only the rest, refused
        manifest / chunk / throwing channel / data not held, 600 chunk data
        without nested rounds), engine test_engine_data (commit then upload,
        commit order of two datas, no second request while on its way, resume
        after reconnect, stale answer ignored, failure left alone until the next
        connect, too big record and short source leave no data, learned chunk
        size, file source, engine without a store), client test_comm_upload
        (comm wiring: answer event, progress events, not_supported without a
        channel) and test_client_sync "data change with record data" (two
        clients over the real server: record commits with its descriptor, author
        reads its data back, the other member sees deferred data of the right
        size), engine test_engine_sync "rebases a change with record data"
        (two clients through the real chain logic: the losing change is rebuilt
        with the same descriptor, its data keeps id, reference and chunks, the
        upload is asked once the rebased record is confirmed, the server's
        record_data index names both datas), core (sources, copy_record_data,
        chunk_enc_size, confirmed_data_in_state).
      Moved: the skip of a download whose content_digest is already held goes to
      RDS 6. It is engine logic at the fetch decision: the held copy can only be
      read through the header of a record naming it, and adopting it means
      re-encrypting with the new data's nonce and key (the encryptor needs a
      fixed-nonce constructor) and checking the result against the manifest
      digest.

+ RDS 4  Data role: server store and upload handling -- done 2026-09-19    [1.5d]
      The data listener (own port), chunk files + data table, ticket
      verification (RD12), manifest verification, data quota (RD10 resource
      side), expiry of incomplete uploads, availability announcements to the
      record servers (RD13). Tests: junk chunk rejected, bad/expired ticket
      rejected, resume reply, quota errors, announcement after completion.
      Scope: both ends of the upload are real over the wire. What needs the
      record server side waits for RDS 5 behind a small interface each: who
      issues tickets (the client channel takes a ticket_source, tests sign with
      a test record server key), where completions are announced (the data
      server calls a complete handler; the announcement packet, the
      availability table and anti-entropy are RDS 5) and the wiring of the
      channel into client_sync (no ticket source to give it yet).
      + data_ticket (core/data/data_ticket): {sid, descriptor (data_id =
        manifest_digest, enc_size, chunk_size), member key id, right
        upload/download, expiry} signed by a record server over a length
        prefixed digest with the context "spsync-data-ticket" (expiry in
        seconds, as it travels). verify(keys, now): signature of the stated
        issuer, not expired; whether the issuer is a record server the holder
        accepts is the holder's check. data_endpoint {host, port, key id,
        region} for the endpoint/holder lists. The digest helpers of
        block_envelope moved to util/digest_buffer.hpp and are shared.
      + data protocol (protocol/data_protocol.hpp, own packet families c2d/d2c
        on the data port 18203): data_hello / data_hello_reply (version),
        upload_data_manifest {ticket, manifest} -> reply {have octets} (open or
        resume), upload_data_chunk {sid, data_id, chunk no, offset, bytes} ->
        reply {complete} (a chunk goes in pieces since RDS 5, see RD4). Download packets come with RDS 6. New protocol errors:
        invalid_data_ticket, data_ticket_expired, invalid_data_manifest,
        invalid_data_chunk, no_such_upload, data_quota_exceeded, data_too_big.
      + server_data_store (server_lib/data_store): one per storage on top of the
        client's record_data_store (same chunk files, table and verification;
        record_data_store::register_data makes a data known by descriptor +
        manifest, data_state_table::total_enc_size), in <root>/<sid>/data.db and
        <root>/<sid>/data/ - a database of its own, so a pure data server needs
        no chain and an all-in-one server keeps the data next to storage.db.
        open_upload -> have-bitmap (the resume reply), store_chunk -> complete.
        Quota (data_quota, server-local, 0 = none): max_data_size ->
        data_too_big; max_storage_bytes -> data_quota_exceeded, counted as the
        enc_size of every known data, so an opened upload RESERVES its whole
        size (uploads in progress cannot overshoot together) and a resume is
        never refused. Expiry: table data_activity {data_id, touched} for
        uploads in progress; expire_incomplete(untouched since) drops rows +
        chunks and frees the reservation, complete data never expires.
      + data_connection (server_lib/data_connection, transport left out like
        connection): ticket check = verify + trusted issuer + issued to the key
        this connection authenticated with + right upload + a descriptor whose
        chunks fit a frame (chunk_size 1..max_chunk_size; the lower bound and
        the match with the committed record are the issuer's business, RDS 5).
        Uploads are opened per connection by (sid, data_id); a chunk for
        anything else is no_such_upload. Completion calls
        data_server_context::announce_complete once.
      + data_server (server_lib/data_server): the listener (certified client
        key required, no anonymous clients), stores opened on demand and those
        found on disk at start, trusted issuers = --record_servers key ids +
        the server's own key (all-in-one issues tickets to itself), expiry at
        start and every expiry_check_interval (10 min) with
        incomplete_upload_expiry (24 h). spsync_server has the role behind
        --data_role with --data_port, --record_servers, --max_data_size,
        --max_storage_data, --upload_expiry; the data root is the storage root.
      + client: net_data_channel (comm/net_data_channel) implements the
        data_channel of RDS 3 over the network: grant (ticket + ordered
        holders) from the ticket_source, the manifest goes to the first holder
        that can be reached - a transport failure moves down the list (RD13
        failover), a refusal by a holder is the answer - one connection per data
        server shared by the uploads going there, the server must authenticate
        with the key the grant names, a lost connection fails the calls still
        out. Known limit: when only the DATA connection drops the upload is
        reported failed and the engine retries after the next record server
        connect (RDS 3 rule); a retry timer belongs to RDS 7 (kill/resume).
      + tests: core test_data_ticket (round trip, expiry, unknown issuer, every
        signed field tampered through the wire form); server_lib
        test_data_store (upload + resume reply, junk/misplaced/oversized chunk,
        foreign or short manifest, contradicting descriptor, both quotas with
        reservation and resume at the limit, expiry across a restart),
        test_data_connection (hello version, eight kinds of bad ticket incl.
        expired, resume on a second connection, chunk without manifest or of
        another storage, junk chunk, one announcement after completion, quota
        errors per storage), test_data_server over TLS with the real uploader
        (end to end incl. chunk-by-chunk equality, resume from ten chunks with
        progress starting there, failover, nobody reachable, no holder, server
        key mismatch, expired / foreign ticket and refusing record server as
        their error codes, expiry at restart) and the data role inside
        spsync_server (off by default, all-in-one with a self signed ticket).
      For RDS 5: chain_sync does not validate descriptors yet - the ticket
      issuer must check the descriptor against the committed record and the
      storage's chunk_size; a separate data server learns the record servers'
      public keys through the link RDS 5 adds (today they must be in its key
      store), the same link carries the announcements.

+ RDS 5  Record role: tickets, endpoints, availability table -- done 2026-09-19 [1d]
      Ticket issue (upload placement, download holder ordering, quota
      accounting), data endpoints + limits in the storage info, the availability
      table fed by data servers and relayed between record servers with the
      heads, load fields. Tests: holder order by completeness/load, endpoint list
      persisted by the client, ticket for an unknown data refused.
      With this step a real client uploads: comm gets tickets from the record
      role and moves the chunks to the data role (test: two clients over an
      all-in-one server, the data becomes in_sync).
      + ticket request: c2s request_data_ticket {sid, data_id, right} ->
        s2c response_data_ticket {ticket, holders}. The data is named by its
        data_id alone: storage::committed_data looks the descriptor up in the
        record data index the record storage keeps (RDS 2) and wants a stored
        record referencing it, so a ticket for an unknown data is refused
        (unknown_data); a syncing replica answers storage_syncing (the record
        may not have arrived yet); no configured data servers ->
        no_data_servers. The ticket is issued to the key of the connection
        (membership checks are distributed phase 7, as for everything else) and
        is valid --ticket_validity (10 min).
      + ticket_issuer + data_availability (server_lib): upload holders =
        placement order, rendezvous hashing of (data server key, data_id) - the
        same on every record server without coordination, even over the set,
        and a server joining or leaving moves only its own share; the first k
        are the primary holders of a copy count k. Download holders = complete
        ones first (fewer uploads in progress, then fewer stored bytes), partial
        ones by how much they have, then the servers nothing is known of in
        placement order (where an upload would have gone), so the table only
        ever improves the answer and need not be complete; holders that are no
        data server of the storage are left out.
      + descriptor bounds are a validity rule now (RD10): valid_data_descriptor
        - sha3-512 digest, chunk_size in [min_chunk_size, max_chunk_size], at
        least one and at most max_data_chunks (200000, a manifest fits a frame)
        chunks - checked by chain_sync on the commit AND the foreign path ->
        invalid_record. chunk_size need not be the storage's (every data is
        self-describing; a client that has not learned the limits yet uses the
        default).
      + storage info: response_sequence_number carries the data endpoint list
        (--data_servers host:port/keyid[/region] on the record servers, an
        all-in-one server lists itself); the engine persists it in the client
        record storage (sync_state.data_endpoints, old tables get the column).
        Transfers use the holders of the ticket answer; the list is the static
        view behind them.
      + availability (RD13): s2s announce_data {holder, entries[{sid, data_id,
        have, total, complete}], stored_bytes, uploads_in_progress}. An
        all-in-one server (storage_server::attach_data_role) puts what its data
        role completes into its own table at once and pushes it to the connected
        peers; a link that comes up gets the whole view in batches of 500, and a
        starting server reads what its data role already holds - the table is
        transient, nothing is persisted. A peer may only announce itself as the
        holder.
      + client: comm makes the net_data_channel itself when the storage has a
        data store (an explicit channel is still taken, for tests); its
        ticket_source sends request_data_ticket over the record connection,
        answers are matched by call id, a disconnect fails the requests still
        out. client_sync needed no change beyond RDS 3's data_root.
      + found by the end to end test with real 1 MiB chunks, both fixed: (1) the
        packet deserialiser caps a message at 1 MiB by default - the data
        listener takes the transport frame limit now (securepath:
        packet_deserialiser forwards the limit constructor); (2) the DER codec
        refuses one octet string above 2 MiB (max_structure_size), so a chunk
        above that could not travel in one packet - solved by the wire pieces
        of RD4 (below), max_chunk_size stays 8 MiB. The same 1 MiB deserialiser default
        sits on the record and s2s connections against 8 MiB record batches:
        recorded in todos.txt, not fixed here. The default data port is 18203
        (18202 is the packet server).
      + tests: server_lib test_data_availability (table, placement: same order
        from any configuration order, spread, minimal movement; download order
        by completeness and load; issuer: unknown data / no servers / no key /
        bad right refused, ticket fields and validity, upload vs download
        holders; endpoint configuration parsing), test_chain_sync descriptor
        bounds on both paths, test_storage committed_data, test_data_announce
        (two all-in-one servers: own table at once, whole view on link up, push
        on completion, load, restart), test_data_server full size chunks; core
        endpoint persistence + sync_state upgrade; client test_client_sync
        "uploads record data" (end to end, availability and both clients'
        endpoint lists checked) and the no-data-servers variant.
      + wire pieces (RD4, decided 2026-09-19 after (2) above): upload_data_chunk
        carries {chunk no, offset, bytes}; protocol constants
        default_data_piece_size 128 KiB and max_data_piece_size 1 MiB with a
        static_assert against the codec limit. Store: chunk_files read_piece /
        append_staged / adopt_staged_chunk, record_data_store::read_chunk_piece
        and begin_chunk -> incoming_chunk (append(offset, piece) hashes and
        stages, finish() verifies against the manifest entry and adopts the
        file; dropped = nothing left behind), server_data_store begin_chunk /
        finish_chunk. data_connection keeps the incoming chunks of an upload
        (at most 32 at once), a piece at offset 0 starts a chunk over, anything
        out of order, overrunning, above the piece maximum or wrong in the end
        is invalid_data_chunk and nothing of the chunk is kept. Client:
        data_channel::send_piece, the uploader walks each missing chunk in
        pieces read from its file, window = 16 pieces (2 MiB on the way per
        data with 128 KiB pieces), progress per piece; the data link sends with
        its lock held so pieces leave in the order they were posted. Tests:
        store pieces (read ranges, receive in pieces, gap, junk, dropped half
        way, already held), connection pieces (interleaved chunks, gap,
        overrun, unknown chunk, junk found at the last piece, restart at 0,
        oversized piece, connection lost half way then finished by the next
        one), uploader pieces (order, window in pieces, short last chunk,
        progress per piece), 8 MiB chunks end to end over TLS.
      Not done, with the reason:
      - quota accounting at ticket issue: a ticket does not say how much will
        move (a resume moves the rest only), counting by ticket overcharges.
        Transfer quota goes where the octets are counted, with serving (RDS 6).
      - the announcement link of a SEPARATE data server: it has to dial the
        record servers' s2s listener, which accepts configured replication peers
        only. Goes with the deployment shapes of RDS 7; until then downloads
        from separate data servers are ordered by placement (where the upload
        went), and their record servers' keys must be in their key store.
      - locality: the region label travels, the ordering does not use it yet
        (the record server does not know where a client is).

+ RDS 6  Download path -- done 2026-09-19                                     [1d]
      Client data connections to the holders, request_data/response_data ranges
      with parallel ranges across holders, notify_data, lazy/auto fetch policy,
      verification on fetch, redirect handling and failover down the holder
      list, transfer quota windows (RD10) on serving, the manifest before the
      chunks, the skip of a download whose content_digest is already held.
      Tests: second client fetches while upload in progress
      (remote_not_complete), completes after notify, holder down -> next
      holder, quota-exceeded fetch resumes in the next window.
      With this step a data goes the whole way: one member uploads, another
      downloads with its own ticket, verifies every chunk and reads the
      plaintext (tests: over TLS, between engines, and three real clients over
      an all-in-one server).
      + data protocol: download_data_open {ticket (download right)} -> reply
        {manifest, have octets}; download_data_piece {sid, data_id, chunk no,
        offset, size} -> reply {bytes, retry_after}. Pull per piece, the same
        wire pieces as uploads (RD4), so serving is stateless beyond the opened
        download and the client's window is the flow control. New errors:
        data_not_held (the data, or that chunk of it, is not here - an upload
        in progress), data_transfer_quota_exceeded. The c2s request_data /
        response_data stubs are dead (RD12: no data through record servers).
      + serving: server_data_store open_download (descriptor must be the one
        the data was uploaded with) and serve_piece (held chunk, range inside
        it, at most a piece); data_connection checks the ticket for the
        download right and the presenting key as for uploads. Transfer quota
        (RD10, all of it here): transfer_budget, served octets per storage and
        window (--transfer_quota, --transfer_window, 0 = none), fixed windows
        aligned to the clock, kept in memory (a restart opens a fresh window);
        what does not fit is not counted, the refusal names retry_after.
      + notify_data (s2c {sid, data_id, complete}): the record role sends it to
        the listeners of a storage when an announcement brings NEWS of a
        complete copy (data_availability::announce says so) - its own data
        role's completion or a peer's.
      + client: data_download_channel (open_download, fetch_piece) next to the
        upload channel, both implemented by net_data_channel (download ticket,
        holders in the record server's order; a holder that is down OR does
        not hold the data is the next entry). data_downloader (comm), the
        mirror of the uploader on the shared action_pump (extracted; the
        uploader uses it too): manifest first - kept only when it is the
        descriptor's - then the chunks missing here and held there, pieces in
        order into record_data_store::begin_chunk, verified when whole. It ends
        with no error when the data is complete here, data_not_held when the
        holder lacked chunks, else with what stopped it; whole chunks are kept
        in every case, so the next fetch goes on from there.
      + comm: fetch_data(data_id) / on_data_downloaded replace the old stubs,
        on_data_available relays notify_data, progress events with upload =
        false. The real channel is made by comm as for uploads.
      + engine: object data of others is deferred until asked for (RD6 lazy):
        engine_input::fetch_object_data(record, change) makes it
        download_pending and asks comm. sync_engine_config::auto_fetch_max_size
        (0 = lazy) marks small enough data wanted when its record arrives.
        Downloads are asked like uploads - confirmed_data_in_state(
        download_pending) after every record event and after a (re)connect, so
        a lost connection resumes. Answers: complete -> in_sync (the store flips
        it with the last chunk) + on_data_state_changed; data_not_held ->
        remote_not_complete, and notify_data makes it download_pending again;
        anything else -> on_data_transfer_failed, left alone until the next
        connect or until it is asked for again.
      + content_digest skip: the data table keeps the members-only header once
        a record carrying it was read (it lives in the local database like the
        group keys) and indexes its content digest. fetch_object_data looks for
        a complete data with the same content under another data id and
        rebuilds the wanted one from it - data_encryptor with the wanted nonce
        and group key gives the same chunks again, taken only when they hash to
        the wanted manifest digest (record_data_store::adopt_content; D9 key
        candidates tried in turn) - outside the engine mutex, like the create
        path. No request leaves the client.
      + tests: server_lib data connection download (wrong right, unknown data,
        contradicting descriptor, pieces of a held chunk, chunk not here yet,
        ranges) and transfer quota (window, retry_after, per storage),
        transfer_budget; comm test_data_downloader (whole data, window and
        piece order, partial holder then the rest, quota and lost connection
        half way, forged manifest, junk chunk, refusal, queue); TLS
        test_data_server (upload by one client and download by another, read
        back; failover; nobody uploaded; somebody else's ticket; a two second
        quota window that takes more than one try); engine fetch (on demand,
        remote_not_complete then notified, failed then asked again, reconnect,
        evicted, same content not downloaded) and engine sync "transfers
        record data" (three engines: lazy, auto fetch through
        remote_not_complete + notify, author never fetches); client "transfers
        record data" (three clients, real server: upload, lazy fetch, auto
        fetch with the server's notify_data - seen in the log); core content
        adoption and table upgrade.
      Not done, with the reason:
      - parallel ranges of one data from several holders: one holder per
        download with failover for now. It needs a per-holder piece scheduler
        in the downloader and deployments with several complete holders to
        matter (RDS 10 replication).
      - automatic retry after a transfer quota refusal: the fetch ends with the
        error and what it got, the next fetch goes on; a timer that uses
        retry_after belongs with the upload retry of RDS 7.
      - auto fetched data does not try the content skip (there is no header at
        hand without decrypting the record; it applies to data that is asked
        for).

+ RDS 7  End to end -- done 2026-09-21                                        [1d]
      System test: multi-hundred-MB object between two clients over the test
      server incl. kill/resume both directions; mobile-style lazy fetch of one
      object among many; the two deployment shapes of RD12 (record servers +
      separate data server, all-in-one replicas); the announcement link of a
      separate data server (left by RDS 5); transfer retries that do not wait
      for a record server reconnect (left by RDS 4 and RDS 6).
      + system test (client test_record_data_system, real clients, a real
        all-in-one server, a file as the source): the author loses its
        connection in the middle of the upload and comes back - the server kept
        what had arrived and the upload goes on from there; the reader loses its
        connection in the middle of the download - state download_pending, the
        whole chunks kept, resumed on reconnect - and reads the content back
        (sha3 compared in pieces). Then twenty objects of which the reader asks
        for one: that one is in_sync, the others stay deferred with nothing
        held. "record data system transfer" [system] runs with the suite with
        48 MiB (about 10 s), "... of a big object" [.system] is the same with
        300 MiB (about 30 s, run explicitly). Both interruptions are asserted
        to have happened mid-transfer.
      + a record connection that goes takes the data connections along: comm
        closes its net_data_channel on disconnect (tickets come over the record
        connection; a killed client drops both anyway).
      + transfer retry (comm/transfer_retry): a transfer that ended with an
        error another try may lift is tried again by comm after a wait, with
        its handle kept - the owner hears once, when it ended for good. Wait =
        what the error says, else the data's backoff (1 s doubling to 60 s,
        forgotten when the data's transfer ends for good); cancelled on
        disconnect (the engine asks again after the reconnect).
        retryable_transfer_error: everything except what another try would
        repeat (refused ticket, manifest or chunk that does not verify, unknown
        data, no data servers, not supported) and data_not_held, which is the
        owner's news and ends with notify_data. The transfer quota's retry_after
        reaches the retry: protocol::make_retry_error / retry_after carry the
        seconds in the error.
      + uploads fail over on a full data server as well: data_quota_exceeded and
        data_too_big move to the next holder of the grant (RD13: a data server
        at its quota behaves as if it was not on the ring for new data).
      + separate data server (RD12 shape two): --record_servers is now
        host:s2s_port/keyid - the data server trusts their tickets AND keeps a
        link to each (server_lib/record_server_link): dials the record server's
        s2s listener with backoff reconnect, says peer_hello, hands the record
        server's handshake-verified key to its key store (how a separate data
        server learns the keys its tickets are signed with) and announces -
        the whole view when the link comes up, each completion as it happens.
        An entry without a host is trusted for tickets only. The record server
        runs its s2s listener also when a data server other than itself is
        configured, accepts a configured data server's key there
        (storage_server_context::is_data_server) and marks the connection: a
        data server link gets no heads, pushes or announcements and everything
        but announce_data from it is ignored. The announcement builders moved to
        data_server (announcements / announcement) and serve both shapes.
      + tests: comm test_transfer_retry (retry_after parsing, retryable errors,
        backoff doubling and cap, hint, forget, cancel, owner gone while
        waiting), client "comm retries transfers" (upload retried after a lost
        data connection and reported once, refusal reported at once, download
        after the quota's retry_after, a wait running at detach), server_lib
        "separate data server" (link up after the record server starts, key
        learned, upload with the record server's ticket, announcement and load
        in its table, download by another client, restarted record server gets
        the whole view) next to the all-in-one "data availability is announced
        to the peers" of RDS 5.
      Not done: a kill of the SERVER in the middle of a transfer as a system
      test (covered piecewise: the store keeps whole chunks and expires the
      rest, links and clients reconnect with backoff); real processes instead of
      in-process servers (scripts/multi_server_env.sh knows no data role yet).

+ RDS 8  Chain validity limits -- done 2026-09-07                          [0.5d]
      max_record_size and chunk_size as per-storage creation parameters (RD10
      validity side, RD3): wire + persistence next to storage_modes, chain_sync
      validation, record_too_big error, limits reported to clients, engine
      pre-check. Size-aware record batching in response_records (fixes the
      frame-cap wedge hazard, RD10). Independent of the data work; wanted before
      distributed phase 4 and now overdue - do it first. Tests: oversized record
      rejected identically on reopen, mismatching creation limits refused, a
      range of large records still fetchable.
      Implemented: storage_limits {max_record_size, chunk_size} inside
      storage_modes (sync_mode.hpp; defaults 256 KiB / 1 MiB, ranges 4 KiB..8 MiB
      and 256 KiB..8 MiB, valid_storage_limits; a chunk above the codec's 2 MiB
      octet string limit is no problem since chunks travel in pieces, RD4). 0 = not stated: the server
      defaults at creation (storage_config::default_limits, spsync_server
      --max_record_size / --chunk_size), "whatever the storage has" when a client
      states its expectation (modes_match, which also covers the unasserted
      replication mode). Wire: create_storage and response_sequence_number carry
      both limits, peer_heads/push_records carry them with the modes so a replica
      creates its copy with identical limits. Persisted in storage_config (old
      databases get the columns added and the compiled defaults, the same on
      every replica). chain_sync: too_big() on the commit and the foreign path ->
      protocol::errc::record_too_big; the limit is over the authenticated record
      content (chain_block::record_bytes), signature and framing (~ a few KiB)
      come on top. Batching: chain_log::get / get_by_origin take a byte budget
      (chain_sync_config::max_response_bytes, 8 MiB) with an 8 KiB per record
      allowance, always at least one record - clients and peers walk ranges as
      before, they just get shorter batches for large records. Client: comm
      delivers the limits in sequence_info (the sequence request is the per
      connection attach exchange: expected modes out, actual modes + limits back;
      the server id rides there too because that answer is where the engine
      decides about a replica switch). The engine learns them once and persists
      them in the record storage's sync_state next to the cursor owner, so an
      oversized object/user/segment change is refused with
      sync::errc::record_too_big before creating the record - offline and right
      after a restart as well; a later answer with other limits is logged as a
      misconfigured replica, the stored ones win. When the RD12 data endpoints
      join the exchange the packet pair should be renamed to an attach/storage
      info exchange; not worth the churn now.
      Tests: storage "limits are persisted and immutable", chain_sync "record
      size limit and size aware batching" (rejection on both paths and after a
      reopen, 41 x ~3 KB records under a 64 KiB budget fetched in full), engine
      "refuses oversized records" (incl. offline after the limits were learned),
      record_storage "limits persist", s2s replica creation compares the filled
      limits.

- RDS 9  Retention / segment interaction                                       [1d]
      With segments.txt SEG 5 (done 2026-09-01): prune data at segment cut,
      retained live objects keep theirs; server GC by data_id references.

- RDS 10 Replication among data servers                                        [2d]
      Copy count k with hash placement, availability exchange between data
      servers over the s2s link (reduced packet set, no heads), pulls by
      manifest, the all-in-one setup where every replica has both roles
      (distributed phase 4 done 2026-09-04, so this is unblocked).

Ordering: RDS 8 first (done). RDS 1-7 are single-server work with the data role
split in from the start; they unblock file sync (use case 1) and groupchat
attachments. RDS 9 and 10 follow.


Resolved questions (2026-09-07)
-------------------------------

q: inline threshold - keep the metadata path for small payloads (group chat
   messages today), no inline field in the descriptor: one mechanism per size
   class keeps the chain small. Hard bound is max_record_size (RDS 8), the
   advisory application cutoff a fraction of it (~32 KiB). Header padding (RD11)
   gives inline payloads the same size hiding as messages.
q: dedup by content_digest - no server-side dedup: data_id depends on the
   per-data nonce, so two uploads of one file are unrelated to the server by
   construction, which hides that two members share a file (convergent
   encryption would give it away). content_digest stays in the encrypted header
   for the client: a client holding a file with that digest skips the download.
   Local reference counting by data_id (RDS 2). Full content addressing deferred.
q: compression - a reserved flag in the encrypted header, client-side only,
   implementation deferred. If it comes: per chunk (random access survives),
   then pad, then encrypt; most large payloads are media and gain nothing.
q: chunk_size - per storage, immutable creation parameter next to the modes and
   the validity limits (RDS 8), range 256 KiB .. 8 MiB; the descriptor still
   carries it so every data is self-describing. The chunk is the unit at rest
   only: on the wire it moves in pieces (RD4), so neither the frame cap nor the
   codec's 2 MiB octet string limit bound it.
q: transfer scheduling - two datas in flight per connection, a window of 4-8
   chunks each; explicit fetches first, then auto-fetches newest first, uploads
   in commit order; no bandwidth caps in the engine (phase 7 rate limiting);
   resume is free through the manifest exchange (RDS 3, RDS 6).
q: fetch access control - membership is enough; the meaningful later split is
   upload rights versus read, not history versus data; wired to the phase 7
   rights when they exist.
q: per-member quotas - server-local only, attributed by the connection's
   authenticated key id for uploads and by the record signer for chain growth;
   only per-storage limits are advertised to clients; after phase 7 rights.
q: replication of data - not a storage mode: server roles, copy count k as
   server policy, no proxying (RD12, RD13).


Open questions
--------------

q: per-storage override of the server-side data policy (k, quota) - operator
   configuration by storage id pattern, or a hint from the creating client in
   the storage info?
q: region labels on endpoints and their use for holder ordering - needed for the
   first deployments or later?
q: should the data role keep a small cache of foreign data it served as a
   proxy fallback (RD12), or serve and forget?
