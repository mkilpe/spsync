
--- phase half
+ user change, just what is required for inital record
+ single data change without previous oid and without actual data (only metadata)
+ system tests without network


--- phase 1 (minimal working state with single change (initial) per oid)
+ server
+ client networking
+ internal conflicting record handling when committing to server (out of sync handling)
* full user change record support (new enc key distribution works; conflicting key sequences are first-win, merge semantics in distributed_sync.txt D9)
+ multichat PoC (groupchat app: channels, invite/join/message/reload, cli client, system tests)


--- phase 2 (multi change per oid and per record support)
+ previous oid support (server validation + per-change rebase, 2026-08-27)
* record data support (design + steps: record_data.txt; descriptor/chunk crypto, chain limits, client data store, engine create path + upload queue, data server + client data channel done, RDS 1-8, 2026-09-21 - a data goes the whole way between clients, both deployment shapes, interrupted transfers resume, system test with a 300 MiB object; retention/segment interaction and replication among data servers open, RDS 9/10)
+ user conflict handling (conflict_policy rebase_on_top/ask + on_object_conflict, 2026-08-27)

--- phase 3 (full record support)
- segment records (plan: segments.txt, 2026-08-30)
- server member access checking (distributed_sync.txt phase 7)
* multi data change records (validated and rebased end to end 2026-08-27; engine input api still creates only single-change records, collation open)

--- phase 4 (full handling and progress support)
- progress
- recording of all actions
- side channel support (send non-persistent packets to all storage members)

--- phase 5
- full documentation / examples
- more tests

--- phase x (specific use case)
- file synchronisation
